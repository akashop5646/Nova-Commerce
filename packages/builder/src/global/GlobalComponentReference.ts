export class GlobalComponentReference {
  public id: string;
  public globalComponentId: string;

  constructor(id: string, globalComponentId: string) {
    this.id = id;
    this.globalComponentId = globalComponentId;
  }
}
