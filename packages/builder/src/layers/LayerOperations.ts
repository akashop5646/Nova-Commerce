export class LayerOperations {
  public moveLayer(id: string, index: number, parentId?: string): void {
    // Reorders visual layers layout coordinates
  }

  public toggleVisibility(id: string): void {
    // Shows/hides specific node in layers trees
  }
}
