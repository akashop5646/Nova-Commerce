export class PublishManager {
  public async compileAndDeploy(websiteId: string, layoutState: any): Promise<boolean> {
    // Compiles layout tree files and deploys to visual preview hosting CDN
    return true;
  }
}
