export class RegistryPublisher {
  public async publishRegistryIndex(indexJson: string): Promise<boolean> {
    console.log("Publishing index manifest registry configurations...");
    return true;
  }
}
