# Playbook: Create Theme

Instructions to define styling configurations:

1. **Scaffold Folder**: Setup theme config JSON structures inside `packages/theme/`.
2. **Populate Tokens**: Map primary palettes, secondary colors, typography, buttons and cards variables.
3. **Compile Variables**: Verify theme resolver parses variables without errors.
