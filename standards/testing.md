# Testing Standard

Every package and application must maintain a baseline test harness.

## Standards
- Component tests should check props mapping correctness.
- Code changes must run in automated linting and typescript compilation cycles.
- Registries must check dependencies loop detection inside validation tests.
