# Block Package Standard

Blocks represent structural segments composed of UI Components (e.g. Hero banner containing Button elements).

## Package Requirements
- Expose Puck schemas detailing allowed sub-components.
- Must only depend on packages from the UI layer (`@klin/ui`) or core modules.
- Blocks must be versioned and include transition animations presets.
