# Tasks — Phase 01

- [x] Configure pnpm workspaces structure (`pnpm-workspace.yaml`).
- [x] Initialize Turborepo tasks structure (`turbo.json`).
- [x] Set up permanent shared tooling config dirs (`tooling/`).
- [x] Setup GitHub CI/CD workflows under `.github/workflows/`.
- [x] Document ARCHITECTURE.md outlining allowed dependency boundaries and 10 Golden Rules.
- [x] Write all core files in `standards/` and `playbooks/`.
- [x] Add initial Architecture Decision Records in `docs/adr/`.
- [x] Add boilerplate placeholder code inside apps and core packages.
- [x] Run compilation build and verify it succeeds.
